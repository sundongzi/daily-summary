##### 威胁总览（截止时间10.30日）

1. 威胁检测/威胁总览菜单
2. 事件统计组件
2. ~~基本信息编辑（部门/负责人改成远程搜索）~~
2. ~~数据更新时间（初始进入页面时为当前时间，后续随着刷新频率变化）~~

##### 恶意代码扫描（单独模块11.2）

1. ~~数据更新时间是啥（初始进入时为当前时间，后续跟着刷新频率更新）~~
2. ~~检测时间是啥（初始进入时为当前时间，后续每十秒更新一次）~~
3. ~~列表周期选择会影响左侧树么（会）~~
3. ~~数值格式化~~
4. saveAgentScanTask

##### 恶意代码隔离（11.3）

1. 

##### agent监控（截止时间10.31日）

1. ~~阈值设置，去掉必填，进行数字校验 阈值 1-100 时间为0至1天~~
1. 左侧树页面刷新时需要维态
1. ~~左侧树修改为层级~~
1. 资源监控列表cpu缺少percent字段

##### 资产中心（11.4）

1. ~~资产发现，前端页面开发完成，待联调~~
1. ~~ip缺少格式校验~~
1. 缺少导出接口

````
<el-tooltip placement="top">
            <div slot="content">
              <ul
                v-if="
                  scope.row.subjectType == 'Windows' ||
                  scope.row.subjectType == 'Linux'
                "
              >
                <li>主机IP：{{ scope.row.hostIp }}</li>
                <li>主机名：{{ scope.row.extendInfo.hostName }}</li>
                <li>业务组：{{ scope.row.extendInfo.groupName }}</li>
              </ul>
              <ul v-if="scope.row.subjectType == 'Container'">
                <li>
                  容器名：{{
                    scope.row.extendInfo.containerName
                      ? scope.row.extendInfo.containerName
                      : scope.row.extendInfo.hostName
                  }}
                </li>
                <li>所属pod：{{ scope.row.extendInfo.containerPod }}</li>
                <li>
                  所属业务组：{{ scope.row.extendInfo.containerBusinessGroup }}
                </li>
                <li>所属集群：{{ scope.row.extendInfo.containerCluster }}</li>
                <li>所在主机IP：{{ scope.row.hostIp }}</li>
                <li>所在主机名：{{ scope.row.extendInfo.hostName }}</li>
                <li>
                  所在主机业务组：{{ scope.row.extendInfo.hostBusinessGroup }}
                </li>
              </ul>
            </div>
            <span>
              <i
                class="iconfont cl-windows"
                :class="scope.row.extendInfo.status == 0 ? 'alive' : 'die'"
                v-if="scope.row.subjectType == 'Windows'"
              ></i>
              <i
                class="iconfont cl-Linux"
                :class="scope.row.extendInfo.status == 0 ? 'alive' : 'die'"
                v-if="scope.row.subjectType == 'Linux'"
              ></i>
              <i
                class="iconfont cl-docker"
                :class="
                  scope.row.extendInfo.containerStatus == 0 ? 'alive' : 'die'
                "
                v-if="scope.row.subjectType == 'Container'"
              ></i>
            </span>
          </el-tooltip>
          <span v-if="scope.row.subjectType != 'Container'"
            >&nbsp;&nbsp;{{ scope.row.hostIp }}</span
          >
          <span v-else
            >&nbsp;&nbsp;{{
              scope.row.extendInfo.containerName
                ? scope.row.extendInfo.containerName
                : scope.row.extendInfo.hostName
            }}</span
          >
          <div
            v-if="
              scope.row.alertType == '端口扫描' ||
              scope.row.alertType == '反弹shell' ||
              scope.row.alertType == '暴力破解'
            "
          >
            <span class="identifying"
              ><i class="iconfont cl-gongjiIP"></i
            ></span>
            <span class="identifytext">{{ scope.row.remoteIp }}</span>
          </div>
          <div v-if="scope.row.alertType == '异常登录'">
            <span class="identifying"><i class="iconfont cl-yuanIP"></i></span>
            <span class="identifytext">{{ scope.row.remoteIp }}</span>
          </div>
        </template>
````

```

```

